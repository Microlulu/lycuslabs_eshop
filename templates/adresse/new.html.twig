{% extends 'base.html.twig' %}

{% block title %}New Adresse{% endblock %}

{% block body %}
    <h1>Add Address</h1>
    <div class="forms_container">
            <div class="col">
                {{ include('adresse/_form.html.twig') }}
                <a class="button_inset" href="{{ path('profil') }}">Back to profil</a>
            </div>
    </div>


{% endblock %}
