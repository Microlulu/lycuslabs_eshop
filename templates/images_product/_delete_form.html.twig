<form method="post" action="{{ path('images_product_delete', {'id': images_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ images_product.id) }}">
    <button class="btn">Delete</button>
</form>
