{% extends 'base.html.twig' %}

{% block body %}
	<main>
		<div class="d-flex container mt-5 ">
			<div class="row">
				<div class="col-6">
					<div class="pro-img-details">
						<img src="{{ asset('images/products/' ~ product.image)}}" class="img-fluid rounded-start" style="width:600px" alt="velo"></td>
				</div>

				<div class="row-6 mt-4">
					{% for image in produit.images %}
						<img src="{{ asset('images/products/' ~ image.image)}}" alt="velo" style="width:100px">
					{% endfor %}
				</div>
			</div>
			<div class="col-6">
				<h4 class="pro-d-title">
					<h2>{{ product.title }}</h2>
				</h4>
				<p>
					{{ product.description}}
				</p>
				<div class="product_meta">
					<span class="posted_in">
						<strong>Category:</strong>
						<a rel="tag" href="#">{{ product.category.name }}</a>,
					</span>
				</div>
				<div class="m-bot15">
					<strong>Price :
					</strong>
					<span class="pro-price">{{ product.price }}
						€</span>
				</div>
				<p>
					<a class="btn btn-primary mt-2" href="{{ path('add_cart' , { 'id' : product.id }) }}">
						Add to cart
					</a>
				</p>
			</div>
		</div>
	</main>
{% endblock %}
