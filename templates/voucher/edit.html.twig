{% extends 'base.html.twig' %}

{% block title %}Edit Voucher{% endblock %}

{% block body %}
    <h1>Edit Voucher</h1>
<section>
    <div class="card_dash">


    {{ include('voucher/_form.html.twig', {'button_label': 'Update'}) }}
<div class="buttons_panel">
    <div class="row">
        {{ include('voucher/_delete_form.html.twig') }}

        <a class="button_inset_back" href="{{ path('voucher_index') }}">back to list</a>
    </div>
</div>
    </div>
</section>
{% endblock %}
