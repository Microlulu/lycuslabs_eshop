<article class="service_container left">
    <div>
        <div>
            <h2>{{ item.title }}</h2>
            <h3>{{ item.subtitle }}</h3>
        </div>
        <div class="row-2">
            <div class="imgServ_Box">
                <img src="{{ asset('images/services/' ~ item.image) }}" alt="{{ item.shortcut }}">
            </div>
            <div class="left">
                <p>{{ item.text }}</p>
            </div>
        </div>
    </div>
</article>