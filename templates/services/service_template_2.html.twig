<article class="service_container right mr-40">
    <div>
        <div class="txt_right">
            <h2>{{ item.title }}</h2>
            <h3>{{ item.subtitle }}</h3>
        </div>
        <div class="row">
            <div class="right mr-40">
                <p>{{ item.text }}</p>
            </div>
            <div class="imgServ_Box">
                <img src="{{ asset('images/services/' ~ item.image) }}" alt="{{ item.shortcut }}">
            </div>
        </div>
    </div>
</article>