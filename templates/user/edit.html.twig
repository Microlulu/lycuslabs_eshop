{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>
    <section>
<div class="card_dash"
    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="button_inset" href="{{ path('user_index') }}">back to list</a>

    {{ include('user/_delete_form.html.twig') }}
    </div>
    </section>
{% endblock %}
