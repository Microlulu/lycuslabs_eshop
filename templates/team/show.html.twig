{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ team.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ team.lastname }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src="{{ asset('images/team/' ~ team.photo) }}" alt="{{ team.shortcut }}" style="width:100px"></td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ team.job }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ team.description }}</td>
            </tr>
        </tbody>
    </table>
    <a class="button m-20" href="{{ path('team_edit', {'id': team.id}) }}">EDIT</a>

    <a class="button_inset" href="{{ path('team_index') }}">back to list</a>

    {{ include('team/_delete_form.html.twig') }}
{% endblock %}
