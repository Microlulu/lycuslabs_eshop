{% extends 'base.html.twig' %}

{% block title %}New Team{% endblock %}

{% block body %}

<h1>Create new Team</h1>
<section>
    <div class="card_dash">

    {{ include('team/_form.html.twig') }}

    <a class="button_inset" href="{{ path('team_index') }}">back to list</a>
    </div>
    </section>

{% endblock %}
